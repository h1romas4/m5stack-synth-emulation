name: M5Stack CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: docker://espressif/esp32-ci-env
    - name: test
      run: |
        pwd
        ls -laF
        ls -laF /opt
